

<head>
    
    <link href="/Content/smoothslide.css" rel="stylesheet" />
    <link href="/Content/smoothslides.theme.css" rel="stylesheet" />
   
</head>
@model List<MPC.Models.DomainModels.CompanyBanner>
@using MPC.Webstore.Common;
@*<div class="content_area">*@
    @*<div id="wrapper">
        <div id="zoom-slider">
            @foreach (var banner in Model)
            {
                <a id="hlItemUrl" href="@banner.ButtonURL">
                    <img id="imgBanner" src="@banner.ImageURL" />
                </a>
            }
        </div>
    </div>*@
    
    <div class="demo-centering">
        <div class="ss-slides-wrap">
            <div class="ss-slides">
                @foreach (var banner in Model)
                {
                    <div class="ss-slide">
                        <img src="@banner.ImageURL" />
                    </div>
                }
            </div>
        </div>
    </div>

    @*</div>*@

  <script type="text/javascript" src="http://ajax.googleapis.com/ajax/libs/jquery/1.8.3/jquery.js"></script>  
 <script type="text/javascript" src="/Scripts/smoothslides.js"></script>
<script type="text/javascript" src="/Scripts/smoothslides.min.js"></script>
    <script type="text/javascript">
        //$.noConflict();
        // $(window).load(function () {
      
      //  $(document).ready(function () {
          
        $(window).load(function () {
        
            $(document).smoothSlides({
                duration: 5000
                
            });
          
       });
       // });
        
    </script>
